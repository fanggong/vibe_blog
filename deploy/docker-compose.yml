services:
  nginx:
    image: nginx:stable-alpine
    container_name: vibe_blog_nginx
    ports:
      - "80:80"
    volumes:
      - /srv/vibe_blog:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
